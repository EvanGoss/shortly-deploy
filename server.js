// This file has been modified. Bad, bad, bad.

var app = require('./server-config.js');

var port = 4568;

app.listen(port);

console.log('Server now listening on port ' + port);

var something = 'totally a thing';